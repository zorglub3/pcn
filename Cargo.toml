[package]
name = "pcn"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = { version = "0.10.2", features = ["thread_rng"] }
rand_distr = { version = "0.6.0" }
serde = { version = "1.0.228", features = ["derive"] }
dense_matrix = { path = "../dense_matrix" }
num-traits = "0.2.19"
